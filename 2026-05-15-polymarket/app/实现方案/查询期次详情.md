# 开发
1. 数据库:mysql
2. 开发语言:java

# 要求
1. 执行“连接数据库”加载skill,通过提供的表读取表结构
2. 一定要遵循现有代码的规范
3. 关键位置和方法需要中文注释
4. SQL需要有一个很好的可读性格式
5. 写代码时一直执行，无需我确认
6. 禁止修改表结构,如果必须修改表结构只给出sql语句
7. 响应数据中的Long 、BigDecimal 都必须转化成String类型
8. 非特殊说明全部使用post请求
9. 不能修改现有业务逻辑
10. 所有参数和响应字段都需要使用驼峰命名
11. 请求中除tinyint、Int、BigDecimal 外其他全部用string类型
12. 项目入口:doge-app-api

# 接口
本接口使用Get请求

## 查询期次详情（已完成）
* 表oracle_issue
### 请求参数
ID 期次ID
### 响应字段
* ID id
* issue_code 期次编码
* symbol 币对
* baseCoin 基础币
* begin_time 开始时间
* end_time 结束时间
* open_price 基准价
* close_price 结束价
* start_advance_minutes 参与开始提前分钟数
* cutoff_advance_minutes 参与截止提前分钟数
* marketType 市场类型
* status 状态(1=进行中,2=待开始,3=待开奖,4=已开奖)
* statusName 状态名
* fee_rate 手续费率
* total_effective_amount 有效参与金额
* positive_amount 正方(涨方)金额
* negative_amount 反方(跌方)金额
* limit_mode 限制模型(1=仅1次,2=不限制)
* allocType 净清算池类型
* allocTypeName 净清算池名称
* buyBeginTime 购买开始时间
* buyEndTime 购买结束时间
* currentTime 当前时间
* klinePeriod K线周期
* buyInfo 购买信息(集合,如果未登录返回空集合)
*    choiceValue 涨/跌
*    amount 数量
* balance USDT余额(如果未登录返回空)
* isParticipate 是否参与(true=是,false=否,如果未登录返回false)



# 调整内容
1. 增加响应字段baseCoin、close_price、marketType、balance（已完成）
2. isParticipate 是否参与(true=是,false=否,如果未登录返回false)（已完成）
3. balance 用户资产余额(USDT,如果未登录返回空)（已完成）
4. 调整响应接口,新增了一些字段（已完成）
   * ID id
   * issue_code 期次编码
   * symbol 币对
   * begin_time 开始时间
   * end_time 结束时间
   * open_price 基准价
   * start_advance_minutes 参与开始提前分钟数
   * cutoff_advance_minutes 参与截止提前分钟数
   * status 状态(1=进行中,2=待开始,3=待开奖,4=已开奖)
   * statusName 状态名
   * fee_rate 手续费率
   * total_effective_amount 有效参与金额
   * positive_amount 正方(涨方)金额
   * negative_amount 反方(跌方)金额
   * limit_mode 限制模型(1=仅1次,2=不限制)
   * isParticipate 是否参与(true=是,false=否,如果未登录返回false)
   * balance 用户资产余额(USDT,如果未登录返回空)
   * marketType 市场类型
   * baseCoin 基础币
   * close_price 结束价(当前价)
   * allocType 净清算池类型
   * allocTypeName 净清算池名称
   * buyBeginTime 购买开始时间
   * buyEndTime 购买结束时间
   * klinePeriod K线周期
   * buyInfo 购买信息(集合,如果未登录返回空集合)
   *    choiceValue 涨/跌
   *    amount 数量 k
5. negativeAmount、positiveAmount、totalEffectiveAmount 不能使用科学计数法返回（已完成）
6. closePrice、openPrice、feeRate、negativeAmount、positiveAmount、totalEffectiveAmount去除末尾0（已完成）
7. allocType 返回为空了,如果为空返回枚举OracleAllocTypeEnum中第一个,需根据语言返回（已完成）
8. 已登陆用户并且有USDT资产,但balance没有值，接口调整为可匿名访问且可识别登录态，并补充请求头 Token 兜底解析，带 Token 时正常返回用户余额（已完成）
9. buyInfo.amount 去除末尾0（已完成）
10. 增加字段`min_pay_amount` 最小付款金额(USDT)', `max_pay_amount`最大付款金额(USDT)'（已完成）
11. 增加amountList字段,通过min_pay_amount和max_pay_amount计算出5段，amountList 只返回整数（已完成）
12. 增加字段kline_period字段（已完成） 
13. 增加返回字段 currentTime(当前时间)（已完成）
